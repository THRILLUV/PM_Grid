# PM Grid 작업 규칙

구현은 사용자가 **구현**이라고 한 뒤에만 한다.
그 전에는 스펙만 적고, 파일을 고치지 않는다.
한 번에 기능 하나만 구현한다.

## 진행 방식

1. 사용자가 원하는 기능 하나를 말한다.
2. 나는 범위 / 포함 / 제외 / 확인 방법을 짧게 적는다.
3. 사용자가 `구현`이라고 하면 그 항목만 만든다.
4. 끝나면 다음 기능을 기다린다.

## 지금 잠근 v0.1

이미 있는 것. 사용자가 다시 열기 전까지 확장하지 않는다.

- 탭: 서비스 플로우, 유저 스토리보드, 정보 구조도, 화면설계서
- 내보내기: PNG, Figma용 SVG, IA 엑셀, PDF(인쇄), JSON 저장/불러오기
- 되돌리기 / 다시실행: 버튼 + Cmd/Ctrl+Z, Shift+Cmd/Ctrl+Z
- IA 오른쪽 표: 드래그 다중선택, Shift+클릭 범위, 행/열 추가삭제, 우클릭 메뉴
- IA 왼쪽 Depth: 우클릭으로 칸/행 추가
- 로컬 경로: `PM_Grid/index.html`
- 미리보기: `http://127.0.0.1:8765/`
- GitHub/Cloudflare 배포: 아직 안 함

## 하지 않는 것 (다음 기능으로 열기 전까지)

- 구글시트 전체 복제 (수식, 시트탭, 열 드래그 이동)
- Jspreadsheet/FortuneSheet 엔진 교체
- 공개 레포에 NodeLab 원문 업로드
- Drive 업로드, Slack 게시

## 대기열

사용자가 하나씩 채운다. 비어 있으면 다음 기능을 기다린다.

1. (비움)
