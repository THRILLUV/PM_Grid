---
description: '캔버스 구현. Frozen identity: this repo main index.html only. Not web-shell. Not PR #6.'
alwaysApply: true
---

# 캔버스 구현

This lock wins over the agency-agents job below. Do not follow React/Vue/Angular defaults from the job when they conflict.

이름: **캔버스 구현**. 정체는 이 레포 `main`의 vanilla `index.html` 한 장이다 (commit `759fa09`, blob `357bba65`). 웹셸 아님. PR #6 아님. 새 코드 없음. 이 파일을 정체로 머근다.

- Repo **THRILLUV/PM_Grid** only. Vanilla `index.html` (and existing `pm-grid-core.test.mjs`). **DO NOT** rewrite the editor as React. No second canvas. No Next wrapper.
- Do not touch **PM_Grid-web**. Do not put a web-shell cursor or web-shell menus (home / all projects / trash / account / pricing) into this canvas file. Do not take canvas code from PR #6 (`cursor/ia-one-sheet-37ce`).
- **F / S**: original editor (nodes + orthogonal H/V rails). **I**: the spreadsheet may stay the team-complete sheet. Work **module by module**. Do not mix F/S/I/V onto one surface. Tabs in this file: `#tab-flow`, `#tab-storyboard`, `#tab-ia`, `#tab-screens`.
- Every new UI string ships Korean and English together (`t(ko, en)` and/or `data-ko` / `data-en`). Seed PM Grid language only (Flow / Story / IA / Screen, 아레나, 취합본). Zero 수능 / 과외 / OCR / 손풀이 / Node_Lab / Manyfast.
- PR title, PR body, commits, and comments are Korean. Code identifiers and filenames stay English.
- **Do not merge.**
